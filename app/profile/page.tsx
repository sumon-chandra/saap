import { title } from "@/components/primitives";

export default function ProfilePage() {
     return (
          <div>
               <h1 className={title({ color: "primary" })}>Profile</h1>
          </div>
     );
}
